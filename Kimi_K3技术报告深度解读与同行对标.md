# Kimi K3 到底强在哪？一篇读懂 2.8T、1M 上下文和 Agent 轨迹工厂

如果只看参数表，Kimi K3 很容易被概括成一句话：

> 2.8T 参数、1M 上下文、原生多模态，又一个“更大、更长”的模型。

但这其实没抓到重点。

K3 真正值得看的地方，不是它把某一个数字推得有多高，而是它把几件过去相对分散的事接到了一起：用混合注意力处理长上下文，用超大稀疏 MoE 扩容量，用原生视觉接入真实环境，再用一整套 Agent 轨迹生产线完成后训练，最后连量化、缓存和推理加速也提前纳入设计。

换句话说，K3 更像一套围绕 Agent 工作负载搭起来的完整系统，而不只是一个更大的基础模型。

不同模型的工具权限、推理预算和 Agent scaffold 未必一致，因此对标数据更适合观察能力结构，不适合简单排座次。

> **阅读地图**
>
> 模型架构 → 1M 上下文 → 原生视觉 → 训练数据 → Agent 轨迹工厂 → 训练与服务 → 同行对标 → 风险与落地路线

---

## 先说结论：K3 的四张牌

如果你只想记住四件事，可以记这四张牌：

1. **KDA + Gated MLA**：大多数层用固定状态压缩历史，少数层负责全局精确检索。
2. **896 专家的稀疏 MoE**：总容量做到 2.8T，但每个 Token 只激活 104.2B 参数。
3. **Agent 轨迹工厂**：任务合成、白盒环境、验证器、异步 RL 和多教师蒸馏连成闭环。
4. **训练与部署共设计**：从 SFT 开始做量化感知训练，服务端再接投机解码、统一缓存和状态复用。

核心规格先放在这里，后面再逐个拆。

| 项目 | K3 配置 |
|---|---:|
| 总参数 | 2.8T |
| 每 Token 激活参数 | 104.2B |
| 最大上下文 | 1M Token |
| 主干深度 | 93 层 |
| 注意力层 | 69 个 KDA + 24 个 Gated MLA |
| 路由专家 | 896 个 |
| 每 Token 激活专家 | 16 个路由专家 + 2 个共享专家 |
| 隐藏维 | 7,168 |
| 注意力头 | 96 |
| 视觉编码器 | MoonViT-V2，27 层、401M 参数 |
| 量化形态 | MXFP4 权重 / MXFP8 激活 |

这里最容易被误读的是第一行：**2.8T 是总容量，不是每生成一个 Token 都要跑完 2.8T。**

---

## 一、2.8T 很大，但 K3 并不打算每次都用完

K3 的主干有 93 层，基本节奏是：

```text
KDA → KDA → KDA → Gated MLA
```

这个组合循环了 24 组，最后一组少 3 个 KDA。

可以把它想成一个负责处理超长材料的团队：

- KDA 像随手更新的工作笔记，便宜、连续、容量固定；
- Gated MLA 像定期回到原始档案库做一次全局检索；
- AttnRes 像跨部门查历史版本，不只看上一层交来的结果；
- MoE 像一个有 896 名专家的顾问池，每次只叫最相关的一小组进场。

这四件事分别处理序列记忆、精确检索、深度信息流和模型容量。

### KDA：把历史压进一个固定大小的状态

标准注意力会为历史 Token 保留 KV Cache，序列越长，缓存通常越大。KDA 换了一个思路：不保存完整历史，而是不断更新一个固定大小的状态矩阵 `Sₜ`。

先把符号讲清楚。对第 `t` 个 Token、某一个注意力头：

```text
x_t             ∈ R^d                 当前层输入
q_t, k_t        ∈ R^(d_k)             query 与 key
v_t             ∈ R^(d_v)             value
S_t             ∈ R^(d_k × d_v)       固定大小的递推状态
alpha_t         ∈ (0, 1)^(d_k)         按 key 通道控制遗忘
beta_t          ∈ (0, 1)               当前头的写入强度
Diag(alpha_t)   ∈ R^(d_k × d_k)        以 alpha_t 为对角线的矩阵
```

KDA 的单头原始递推式是：

```text
S_t = (I - beta_t · k_t · k_t^T)
      · Diag(alpha_t) · S_{t-1}
      + beta_t · k_t · v_t^T

o_t_tilde = S_t^T · q_t
```

第一行看起来有些抽象，把它拆开就清楚了。

**第一步：按通道遗忘旧状态。**

```text
M_t = Diag(alpha_t) · S_{t-1}
```

`alpha_t[r]` 越接近 1，第 `r` 个 key 通道保留得越完整；越接近 0，该通道的旧记忆衰减得越快。它比给整个状态乘一个标量更细，可以让不同地址通道拥有不同的时间尺度。

**第二步：先问旧状态“你认为这个 key 对应什么 value”。**

```text
v_hat_t = M_t^T · k_t
```

这里的 `v_hat_t ∈ R^(d_v)` 是旧记忆对当前 key 的预测。若历史中已经出现过相近的 key，状态应该能够给出一个接近 `v_t` 的结果。

**第三步：只写入旧状态没预测对的部分。**

```text
delta_v_t = v_t - v_hat_t

S_t = M_t + beta_t · k_t · delta_v_t^T
```

`k_t · delta_v_t^T` 是一个 `d_k × d_v` 的外积：它把 value 误差写到由 `k_t` 指向的状态位置。`beta_t` 决定这次修正有多激进。若 `v_hat_t = v_t`，误差为 0，状态就不必重复写入已经掌握的信息。

把 `v_hat_t = M_t^T k_t` 代回去，可以验证它与原始递推式完全等价：

```text
S_t
= M_t + beta_t · k_t · (v_t - M_t^T · k_t)^T

= M_t + beta_t · k_t · v_t^T
  - beta_t · k_t · k_t^T · M_t

= (I - beta_t · k_t · k_t^T) · M_t
  + beta_t · k_t · v_t^T

= (I - beta_t · k_t · k_t^T)
   · Diag(alpha_t) · S_{t-1}
   + beta_t · k_t · v_t^T
```

**第四步：用当前 query 从更新后的状态读取。**

```text
o_t_tilde = S_t^T · q_t
```

K3 还对遗忘率和最终输出增加了稳定化门控。其衰减参数可以写成：

```text
g_t     = g_min · sigmoid(exp(A) · z_t)
alpha_t = exp(g_t)
g_min   = -5
```

`z_t` 是由当前输入生成的衰减控制量，`A` 是可学习参数。由于 `g_t ∈ [-5, 0]`，所以 `alpha_t ∈ [exp(-5), 1]`；这个下界避免单步保留率过小，从而防止短 tile 内的累计衰减落到 BF16 难以稳定表示的范围。

递推读出不会直接送到下一层，还要经过输入相关的输出门：

```text
u_t = sigmoid(W_g · x_t)
r_t = u_t ⊙ RMSNorm(o_t_tilde)
y_t = W_o · r_t
```

其中 `⊙` 表示逐元素乘法。`u_t` 决定哪些输出通道值得保留，`RMSNorm` 控制递推状态长期累积后的幅值，`W_o` 再完成多头聚合后的输出映射。

因此，KDA 不是简单的“线性注意力近似”，更像一个在线误差修正器：

```text
旧记忆先衰减
  → 用当前 key 检查旧记忆
  → 只写入预测误差
  → 用当前 query 读取
  → 经过门控后输出
```

它把任意长度的 Token 历史压进固定大小的 `S_t`，推理状态不随序列长度增长；代价是多个历史片段会共享同一状态容量，不能保证每条原文都能被精确还原。

所以 K3 没有把所有注意力层都换成 KDA。

### Gated MLA：每隔几层，回原始档案库查一次

93 层中有 24 层是 Gated MLA。它仍然对历史 Token 做因果 softmax，只是把需要长期缓存的多头 K/V 先压缩成一个共享 latent。

设头数为 `H = 96`，第 `h` 个头的 content query / key 维度为 `d_h = 128`，共享 KV latent 维度为 `d_c = 512`。

**第一步：联合压缩当前 Token 的 K/V 信息。**

```text
c_t_KV = W_c · x_t
c_t_KV ∈ R^512

c_bar_t_KV = RMSNorm(c_t_KV)
```

服务端真正需要随序列保存的是每个 Token 的 `c_t_KV`，而不是 96 组完整 key 和 value。

**第二步：每个注意力头从共享 latent 恢复自己的 key 和 value。**

```text
q_t^(h) = W_h_Q · x_t
k_t^(h) = W_h_K · c_bar_t_KV
v_t^(h) = W_h_V · c_bar_t_KV
```

`W_h_K` 与 `W_h_V` 让不同头从同一份 512 维压缩表示中读取不同子空间。K3 的 MLA 使用 NoPE，所以上式中的 content query / key 不再额外叠加 RoPE 等显式位置编码。

**第三步：对全部历史 Token 做因果全局匹配。**

```text
score_{t,j}^(h)
= (q_t^(h))^T · k_j^(h) / sqrt(d_h)
  + causal_mask(t, j)

causal_mask(t, j) = 0      , j ≤ t
causal_mask(t, j) = -∞     , j > t

a_{t,j}^(h)
= exp(score_{t,j}^(h))
  / sum_{m=1..t} exp(score_{t,m}^(h))

o_t^(h) = sum_{j=1..t} a_{t,j}^(h) · v_j^(h)
```

这一步仍然是标准 softmax Attention 的核心：当前 query 可以显式比较每一个历史 Token，并把最相关位置的 value 取回来。因此 MLA 能补偿 KDA 固定状态难以完成的精确查找。

**第四步：合并多头结果，并施加输入相关的输出门。**

```text
o_t_tilde = Concat(
  o_t^(1), o_t^(2), ..., o_t^(H)
)

u_t = sigmoid(W_g · x_t)
y_t = W_o · (u_t ⊙ o_t_tilde)
```

这里的 `u_t` 是逐 Token、逐通道的门。它调节的是 MLA 已经算出的输出，而不是决定整层 Attention 是否执行。

MLA 节省缓存的原因也可以直接从规模看出来：

```text
普通多头 KV Cache：
O(T · H · (d_k + d_v))

MLA 压缩缓存：
O(T · d_c)

K3 中 d_c = 512
```

它的缓存仍然随序列长度 `T` 线性增长，但每个 Token 只保留一份低维 latent。换来的好处是，模型仍然拥有 Token 到 Token 的全局 softmax 检索，而不必为 96 个头分别保存完整 K/V。

所以 KDA 和 MLA 不是二选一，而是一对互补组合：

> KDA 负责便宜地记住大意，MLA 负责偶尔回来查原文。

### AttnRes：不只看上一层，也能回看更早的层

KDA 与 MLA 都在处理“时间轴上的 Token 历史”，AttnRes 处理的则是另一个问题：93 层网络越往后走，早期层形成的表示如何避免被一层层覆盖。

传统残差连接通常只连接相邻深度：

```text
h_l = h_{l-1} + F_{l-1}(h_{l-1})
```

其中 `F_{l-1}` 是上一层的 Attention 或 MLP 变换。无论更早层的信息是否仍然有用，它都要先经过连续的相邻残差路径才能到达第 `l` 层。

Full AttnRes 把所有历史层输出都变成可检索的深度来源。先定义：

```text
r_0 = Embedding

r_i = F_i(h_i)                 , i ≥ 1

k_i = v_i = r_i                , i = 0, ..., l-1

q_l = w_l
```

`r_i ∈ R^d` 是第 `i` 个深度来源，`w_l ∈ R^d` 是第 `l` 层学习到的 pseudo-query。key 与 value 都来自历史层输出，但 key 在打分前会做归一化。

完整的深度注意力是：

```text
score_{i->l}
= w_l^T · RMSNorm(k_i)

alpha_{i->l}
= exp(score_{i->l})
  / sum_{j=0..l-1} exp(score_{j->l})

h_l
= sum_{i=0..l-1} alpha_{i->l} · v_i
```

这里的 softmax 不是沿 Token 序列做，而是沿“历史层编号”做。`RMSNorm(k_i)` 只用于计算权重，避免某一层因为向量幅值较大而天然占优；聚合时仍使用原始 `v_i`，保留该层输出的实际尺度与内容。

把传统残差与 AttnRes 并排看，差异会更直观：

```text
传统残差：
h_l = 1 · h_{l-1} + 1 · F_{l-1}(h_{l-1})

AttnRes：
h_l = alpha_{0->l} · v_0
    + alpha_{1->l} · v_1
    + ...
    + alpha_{l-1->l} · v_{l-1}
```

前者的路径和系数是固定的；后者让第 `l` 层根据内容，在 embedding、浅层表示和深层表示之间重新分配权重。虽然 `w_l` 是该层学习到的参数，但 `k_i` 取决于当前 Token 在第 `i` 层的表示，因此最终的 `alpha_{i->l}` 仍会随 Token 内容变化。

如果 93 层全部采用 Full AttnRes，每一层都要保留并检索越来越多的历史来源。K3 因此使用 Block AttnRes：每 12 层形成一个块，块内输出先求和，再把整块当作一个深度来源。

```text
第 n 个完整块：
b_n = sum_{j ∈ B_n} r_j

当前块在第 l 层之前的运行和：
b_n^(<l) = sum_{j ∈ B_n, j < l} r_j

第 l 层可见的深度来源：
V_l = [
  r_0,
  b_0,
  b_1,
  ...,
  b_{n-1},
  b_n^(<l)
]
```

随后对 `V_l` 的每一行执行与 Full AttnRes 相同的打分、softmax 和加权求和：

```text
score_m = w_l^T · RMSNorm(V_l[m])

alpha_m
= exp(score_m)
  / sum_r exp(score_r)

h_l = sum_m alpha_m · V_l[m]
```

K3 共 93 层，按 12 层分块得到 8 个块，最后一块不满。再把 embedding 算作一个来源，最深处最多只需在 9 个深度来源之间选择，而不是逐一检索 93 个层输出。

Attention 子层与 MLP 子层还使用独立的 pseudo-query：

```text
alpha_l_Attn = DepthSoftmax(V_l, w_l_Attn)
alpha_l_MLP  = DepthSoftmax(V_l, w_l_MLP)
```

这意味着同一层里，Attention 可以更偏好某些早期表示，MLP 则选择另一组深度来源。按块聚合把需要长期保存和跨设备传递的深度状态，从逐层的 `O(L · d)` 降到按块的 `O(N · d)`，其中 `L` 是层数、`N` 是块数。

这不是在扩大 Token 上下文。更准确地说：

- KDA 压缩的是**序列轴上的历史**；
- MLA 检索的是**序列轴上的具体 Token**；
- AttnRes 检索的是**网络深度轴上的历史**。

KDA 解决“前文怎么用固定状态记住”，MLA 解决“需要时怎么回查具体原文”，AttnRes 解决“深层网络怎么找回早期表示”。三者处理的是不同轴上的信息流，不能互相替代。

### 896 个专家：容量很大，出场人数很少

> **关键数字**：896 个路由专家；每个 Token 激活 16 个路由专家和 2 个共享专家；路由稀疏比约 56 倍。

专家计算位于 3,584 维 latent 空间，只有主干隐藏维 7,168 的一半。这样既能把总容量推到 2.8T，又不必让每个 Token 跑完整模型。

但专家越多，路由越容易“偏科”：热门专家持续过载，冷门专家长期闲置。K3 使用 Quantile Balancing（QB，分位数均衡）动态校准每个专家的入选门槛。

#### 第一步：计算原始路由分数

对第 `i` 个 Token、 第 `j` 个专家，Router 先计算一个原始分数：

```text
sᵢⱼ = sigmoid((Wᵣ · xᵢ)ⱼ)
```

其中：

| 符号 | 含义 |
|---|---|
| `xᵢ` | 第 `i` 个 Token 的输入表示 |
| `Wᵣ` | Router 参数 |
| `sᵢⱼ` | Token `i` 对专家 `j` 的原始偏好分数 |
| `bⱼ` | 用于平衡专家负载的路由偏置 |
| `n` | 路由专家总数，K3 中为 896 |
| `k` | 每个 Token 激活的路由专家数，K3 中为 16 |
| `m` | 当前全局统计窗口内的 Token 数 |

#### 第二步：带偏置选 Top-16

专家是否入选，由“原始分数 + 负载偏置”共同决定：

```text
Tᵢ⁽ᵗ⁾ = TopKⱼ(sᵢⱼ + bⱼ⁽ᵗ⁾, k)
k = 16
```

`Tᵢ⁽ᵗ⁾` 是 Token `i` 在训练第 `t` 步选中的 16 个路由专家集合。偏置 `bⱼ⁽ᵗ⁾` 只负责改变入选门槛：热门专家可以被适当降权，冷门专家可以获得更多入选机会。

#### 第三步：混合权重不使用偏置

专家被选中后，真正混合专家输出时，权重仍由原始分数 `sᵢⱼ` 决定：

```text
                    sᵢⱼ
pᵢⱼ = ────────────────────────，j ∈ Tᵢ⁽ᵗ⁾
       Σᵣ∈Tᵢ⁽ᵗ⁾ sᵢᵣ

pᵢⱼ = 0，j ∉ Tᵢ⁽ᵗ⁾
```

这一步很关键：`bⱼ` 可以调节“谁有资格入场”，但不会直接改变入场后“谁贡献更多”。因此，负载均衡不会把一个本来匹配度较低的专家强行赋予更高混合权重。

#### 第四步：定义理想负载

如果一个统计窗口内有 `m` 个 Token，每个 Token 选择 `k` 个专家，总共会产生 `m × k` 次路由。平均分配给 `n` 个专家后，每个专家的目标负载是：

```text
    m · k     m · 16     m
q = ───── = ──────── = ──
      n        896      56
```

也就是说，理想情况下，每个专家大约处理全局 Token 数的 `1/56`。

#### 第五步：用第 17 名确定当前入选门槛

在训练第 `t` 步，Top-16 实际使用的是带偏置分数 `sᵢᵣ + bᵣ⁽ᵗ⁾`。K3 再取第 17 大的带偏置分数，作为当前 Token 的路由门槛：

```text
αᵢ⁽ᵗ⁾ = 第 (k + 1) 大的 {sᵢᵣ + bᵣ⁽ᵗ⁾}
       = 第 17 大带偏置路由分数
```

当前步骤中，专家是否跨过入选线可以写成：

```text
current_marginᵢⱼ = sᵢⱼ + bⱼ⁽ᵗ⁾ − αᵢ⁽ᵗ⁾
```

而 QB 在计算新偏置时使用：

```text
QB_marginᵢⱼ = sᵢⱼ − αᵢ⁽ᵗ⁾
```

旧偏置 `bⱼ⁽ᵗ⁾` 不直接出现在 `QB_margin` 中，只通过带偏置门槛 `αᵢ⁽ᵗ⁾` 影响下一步更新。

#### 第六步：用全局分位数计算下一步偏置

K3 希望每个专家大约在 `1/56` 的 Token 上入选，因此对 `QB_margin` 取 `55/56` 分位数，并取相反数作为下一步偏置：

```text
1 − k/n = 1 − 16/896 = 55/56

b̂ⱼ⁽ᵗ⁺¹⁾ = −Q₁₋ₖ⁄ₙ({sᵢⱼ − αᵢ⁽ᵗ⁾ | i = 1, 2, …, m})
          = −Q₅₅⁄₅₆({sᵢⱼ − αᵢ⁽ᵗ⁾ | i = 1, 2, …, m})
```

为了避免所有偏置一起向上或向下漂移，再做一次去均值：

```text
                            1
bⱼ⁽ᵗ⁺¹⁾ = b̂ⱼ⁽ᵗ⁺¹⁾ − ─────── · Σᵣ b̂ᵣ⁽ᵗ⁺¹⁾
                            n
```

直觉上：

- 专家 `j` 经常远高于门槛，说明它太热门，下一步偏置会下降；
- 专家 `j` 经常低于门槛，说明它偏冷门，下一步偏置会上升；
- 经过调整后，每个专家的入选次数逐渐靠近目标负载 `m/56`。

#### 工程上怎么做

系统通过全局直方图近似统计 margin 分布，并用 All-Reduce 聚合不同设备上的结果。新偏置在下一步生效，推理阶段则冻结。

QB 的特点可以概括为：

| 设计 | 作用 |
|---|---|
| 偏置只参与 Top-k 选择 | 调负载，不直接污染专家混合权重 |
| 用第 17 名作为动态门槛 | 直接度量专家距离入选还有多远 |
| 用 `55/56` 分位数更新 | 让每个专家趋近 `1/56` 的目标负载 |
| 全局直方图 + All-Reduce | 在大规模训练中近似计算全局分位数 |
| 推理时冻结偏置 | 保持部署行为稳定 |

> **一句话理解**：QB 不是给 Router 再加一个“请保持均衡”的辅助损失，而是直接估计每个专家应该把入选门槛升高或降低多少。

---

## 二、1M 上下文不是把窗口参数改大就结束了

“支持 1M 上下文”现在已经不是罕见宣传语。真正的问题是：模型是否在训练中见过需要跨越超长距离的信息依赖，以及整个系统是否真的能保存、调度和复用这些状态。

K3 的上下文课程是逐步推进的：

```text
8K 基础预训练
  → 64K 主预训练后段
  → 256K cooldown
  → 1M cooldown + Agent
```

这条路线背后有三类数据。

### 第一类是真实长数据

包括长文档、长视频、跨页材料和大型代码仓库。重点不只是长度，还要尽量保留真实分布与语义连续性。

### 第二类是“必须看远处”的合成任务

如果答案在问题附近就能猜出来，模型即使吃进 1M Token，也未必真的会使用远处的信息。

因此训练数据会通过置换、拼接和多模态组合，故意让正确答案依赖远距离证据。这里真正训练的是“跨长距离找证据”，而不是“能把很长的文本塞进输入框”。

### 第三类是 Agent 的工作记忆

对 Agent 来说，长上下文里不只有文本，还可能同时存在：

- 网页、邮件、长文档和代码仓库；
- 截图、PDF 页面、视频帧和图表；
- 工具返回、错误日志、测试结果；
- reasoning、工具参数、observation 和修正过程；
- 多轮调用留下的环境状态。

于是 1M 上下文就不再只是模型问题，还变成了系统问题：KV/state 要保存，工具日志要持久化，图像 Token 要管理，训练轨迹要能回放，服务端还要尽可能复用已有状态。

这也是为什么 K3 的长上下文设计必须和 Agent 系统一起看。

> **本节 takeaway**：1M 的价值不在“能输入多少”，而在“模型能否在超长状态历史中找到证据、继续行动，并复用已经付出的计算”。

---

## 三、MoonViT-V2 不是后来外挂上去的“眼睛”

K3 的视觉编码器 MoonViT-V2 有 27 层、401M 参数，支持最高 3584×3584 输入。图像经过 2×2 Pixel Shuffle 后，视觉 Token 数量降到原来的四分之一，再与文本进入同一序列。

更重要的是，它不是等语言模型训练完后再接上去，而是从预训练第一阶段就参与统一的 next-token objective。

这带来三个变化：

### 视觉表示直接为生成任务服务

视觉编码器从零训练，不依赖已经完成的对比学习。它学到的不是一套孤立的图像 embedding，而是语言主干能够直接使用的视觉表示。

### 图像和视频共享长上下文

MoonViT-V2 交错使用空间层与时间层，视频端再通过 temporal pooling 降低长视频 Token 压力。

### 视觉可以进入“看—做—验”的 Agent 闭环

代码与 SVG、3D、网页、游戏、CAD 渲染结果被配成训练数据。模型不仅要看懂，还要生成代码、观察渲染结果，再继续修正。

所以原生视觉的价值不只是识图分数更高。它真正打开的是：网页操作、文档处理、界面理解、视觉编程和环境反馈都能进入同一条 Agent 轨迹。

> **本节 takeaway**：视觉不是单独的输入模态，而是 Agent 获取环境状态、执行动作和验证结果的一部分。

---

## 四、预训练数据没公布总量，但思路很明确

K3 公开了**数据类别与构造方法**，但没有公布具体语料库名单，也没有给出每类数据的 Token 数和配比。因此，我们能看清“数据要训练什么能力”，但看不到完整的数据配方。

### 五类预训练数据分别负责什么

| 数据类型 | 公开的数据内容 | 主要训练目标 | 与 K3 路线的关系 |
|---|---|---|---|
| Web | 清洗网页、长文档、跨页信息 | 通用知识、长文阅读、跨段检索 | 为 1M 上下文提供自然长材料 |
| Code | 代码仓库、可执行代码、视觉编程 | 仓库理解、代码生成、执行反馈 | 为 Coding Agent 和“生成—运行—修正”闭环提供基础 |
| Math | 推导、证明、可验证题目 | 多步推理、形式化表达、答案校验 | 适合构造硬验证奖励 |
| Knowledge | 专业知识、结构化材料 | 领域知识、结构化信息理解 | 为专业任务和复杂检索提供内容基础 |
| Vision | Caption、OCR、视频、交错图文 | 识别、跨模态理解、时序理解 | 让截图、文档、视频和环境观察进入统一上下文 |

这五类数据不是互相独立的“桶”。K3 更强调它们之间的组合。例如：

- 网页和代码可以与渲染结果配对，形成视觉编程数据；
- PDF 页面、OCR 文本和跨页信息可以组成文档理解任务；
- 长文档、视频帧、工具日志与 Agent 决策可以被装进同一个 1M 上下文；
- 数学题、代码测试和环境终态可以提供程序化验证信号。

### 长上下文数据是怎么构造出来的

仅有长文档还不够。模型可能把 1M Token 全部读入，却只利用问题附近的一小段。

K3 的长上下文数据包含两条线：

1. **自然长数据**：对长文档、长视频、跨页材料做清洗和上采样，尽量保留真实语义连续性；
2. **强依赖合成数据**：通过置换、拼接和多模态组合，让答案必须依赖远距离证据。

训练长度则按 `8K → 64K → 256K → 1M` 逐级推进。前期先学局部规律，后期再增加真正需要长程依赖的任务。到 1M 阶段，Agent 轨迹也进入课程，让模型学习在很长的观察—行动历史中继续工作。

### 视觉数据不只是图片问答

MoonViT-V2 使用的视觉数据包含 Caption、OCR、视频和交错图文。K3 还使用一类视觉编程数据：代码与 SVG、3D、网页、游戏、CAD 渲染结果配对。

这种数据可以形成一条可执行链：

```text
读取视觉目标
  → 生成代码
  → 渲染或运行
  → 观察结果
  → 根据差异继续修正
```

它训练的不是单纯“看见了什么”，而是“看见之后能否采取动作，并根据新观察继续完成任务”。

### 后训练数据来自轨迹，而不只是问答对

K3 的 Agent 数据大致可以分为三层：

| 数据层 | 生成方式 | 保留的信息 | 用途 |
|---|---|---|---|
| SFT 启动轨迹 | 旧版 Kimi 专家生成，经过自动验证和人工难例标注 | Reasoning、工具参数、observation、最终答案、结构边界 | 建立格式正确、可执行的初始策略 |
| 合成任务与环境 | 从知识 DAG 采样节点或相关组合，检索真实材料后生成任务 | 初始状态、目标、工具空间、预算、验证器 | 扩大任务多样性，构造可复现训练环境 |
| RL 在线轨迹 | Student 在白盒环境中自主探索 | 状态、动作、工具结果、错误恢复、终态和奖励 | 学习长程执行、成本控制与真实错误恢复 |

这里最关键的变化是：一条数据不再只是“问题—答案”，而是一个完整状态转移过程。

```text
初始状态 → 计划 → 动作 → observation → 修正 → 后续动作 → 终态验证
```

因此，轨迹数据的质量标准也不同。它至少要做到：

- 工具动作真的能执行；
- 遇到错误后仍能恢复；
- 最终结果可以独立验证；
- 环境、镜像、工具和 verifier 都有版本，能够重放。

### 哪些数据细节仍然没有披露

| 未披露项 | 为什么重要 |
|---|---|
| 预训练总 Token | 无法直接估算总体训练预算 |
| 五类数据的配比 | 无法判断能力重点主要来自哪类数据 |
| 语言和领域分布 | 无法评估跨语言与长尾领域覆盖 |
| 去重、质量阈值和重复训练策略 | 无法判断数据有效密度与污染风险 |
| Agent 轨迹总量和成功率分布 | 无法估算轨迹工厂的真实规模 |
| GPU 数量、训练 FLOPs 和集群规模 | 无法独立复核成本与 scaling efficiency |

公开信息中，DeepSeek-V4 Pro 的预训练规模为 33T+，GLM-5 基座为 28.5T，而 K3 没有给出对应数字。K3 声称相对 K2 获得约 2.5 倍 scaling efficiency，但这个数字不能直接翻译成“训练速度快了 2.5 倍”，也不能简单说成“成本下降 60%”。

从公开内容看，K3 更想强调的是模型、数据和优化器的联合设计：

- 重新选择 batch、学习率和 tokens-per-parameter；
- 调整模型形状；
- 为 Q/K/V 使用 Per-Head Muon；
- 使用 1% warmup、cosine decay 和 0.1 weight decay；
- 把 1M 上下文放进训练课程，而不是事后扩窗。

> **数据判断**：K3 公开的是数据生产逻辑，而不是完整数据账本。它最有启发性的地方，是把自然语料、合成任务、环境状态和可验证轨迹组织成连续的数据飞轮。

---

## 五、真正有意思的部分：K3 怎么造 Agent 轨迹

如果前面的架构决定了模型“能装多少、能记多长”，那么后训练决定的就是：它到底会不会在真实环境里把事情做完。

K3 的后训练主线是：

```text
高质量 XTML 轨迹
  → SFT
  → 3 个任务领域 × 3 档 effort
  → 9 个 RL 专家
  → MOPD
  → 合并为统一 K3
```

### XTML：让思考、工具和环境反馈说同一种语言

Agent 轨迹里有很多异构事件：

- system、user、assistant、tool 等角色；
- think、response 等输出频道；
- search、python、browser 等动作；
- query、code、URL 等参数；
- 工具结果、状态和错误等 observation。

K3 用 XTML（Extensible Token Markup Language）把这些事件统一序列化。它只保留四个特殊 Token：

| Token | 用途 |
|---|---|
| `[open]` | 打开元素 |
| `[sep]` | 分隔结构 |
| `[close]` | 关闭元素 |
| `[end_of_msg]` | 停止生成 |

`message`、`think`、`response`、`tools`、`call` 和 `argument` 都只是可扩展标签名。

一个简化例子大概长这样：

```text
[open]message role="assistant"[sep]
  [open]think[sep] ... [close]think[sep]
  [open]tools[sep]
    [open]call tool="search" index="1"[sep]
      typed argument ...
    [close]call[sep]
  [close]tools[sep]
[close]message[sep]
[end_of_msg]
```

这样，工具选择、工具名、参数和结构边界都变成了普通的 next-token prediction，不需要为每种工具单独增加一个 action head。

训练时，模型输出参与 loss，环境输入只作为条件：

```text
L_NTP = −Σₜ mₜ · log P(xₜ | x₍<t₎)
```

其中，模型应该生成的 Token 令 `mₜ = 1`；tool result、状态和错误等 observation 令 `mₜ = 0`。

### 好轨迹不是“想得很长”，而是“真的能重放”

K3 对高价值轨迹的定义很务实：

```text
初始环境
  → 计划或假设
  → 工具动作
  → 环境观测
  → 诊断与修正
  → 继续执行
  → 独立验证
```

一条轨迹值不值得训练，不看它写了多少“思维”，主要看四件事：

- **可执行**：工具参数真能在 harness 里运行；
- **可恢复**：看到错误后能回到有效策略；
- **可验证**：奖励来自最终状态或测试，不是语言风格；
- **可复现**：状态、镜像、工具和验证器都有版本。

这很像把 Agent 轨迹当成一个带测试用例的可重放程序，而不是一段聊天记录。

### 白盒环境：把 Agent 产品拆成六块

K3 把 Agent harness 抽象成六类模块：

1. Tools：搜索、文件、终端、浏览器；
2. System：角色、规则、任务边界；
3. Context：裁剪、摘要、recent-k；
4. Skills：领域工作流和工具说明；
5. Memory：跨步骤、跨日的持久状态；
6. Subagents：并行探索和任务委派。

训练时随机化不同 harness 配置，目的是避免模型只会适配某一个产品外壳。它要学的是跨工具语义、上下文管理和子 Agent 结构完成状态转移。

### 自动出题：从知识图谱里长出任务

任务合成不是简单让模型批量改写问题，而是先构建一个自演化的知识 DAG：

1. 从粗粒度知识种子开始；
2. 由 Agent 递归搜索、扩展并去重；
3. 采样单个节点或相关节点组合；
4. 用祖先节点补充检索上下文；
5. 检索论文、博客和代码库等真实材料；
6. 生成 Coding、Knowledge、Vision 等训练任务。

这样可以持续获得不同粒度、不同组合的任务，而不是围绕少量模板反复换皮。

### AET：不给参考轨迹，只给目标和裁判

可验证 Agent 任务需要五个基本要素：

- 初始状态；
- 目标终态；
- 可用工具；
- Token / step 预算；
- 独立验证器。

训练时没有 reference trajectory，Agent 自己探索路径。Public verifier 可以提供诊断和纠错，Hidden verifier 则负责最终考试，不向模型泄露测试细节。

奖励优先看真实终态：

```text
正确性门槛 → 质量或性能 → anti-hack
```

失败、伪造或环境错误归零。

这类训练的关键不是教模型复刻一条“标准操作”，而是让它在环境里自己找到能通过验证的策略。

### 3×3 专家：领域和推理预算分开学

K3 训练了 3 个领域 × 3 档 effort 的九个专家：

| 领域 / Effort | Low | High | Max |
|---|---|---|---|
| General | 经验、知识、视觉；短推理 | 复杂推理、搜索、答案验证 | 困难科学、忠实性、最大探索 |
| General Agent | 日常助理、短工具链 | Deep Research、多阶段写作 | 长程工作流、多阶段交付 |
| Coding Agent | 小修复、短测试 | SWE / Web Dev、多轮调试 | Kernel / 大仓库、最大预算 |

为什么不一开始就练一个全能模型？

因为不同任务的奖励密度、轨迹长度、工具环境和最佳预算差异太大。把所有任务塞进同一个 RL 过程，容易相互干扰。

### MOPD：最后再把九个专家装回一个模型

部署侧不希望维护九套模型，所以 K3 用多教师在策略蒸馏（MOPD）合并。

流程是：

1. Student 用当前策略自己生成 rollout；
2. 根据任务领域和 effort 匹配一个 Teacher；
3. Teacher 不负责生成，只对 Student 的 Token 打分；
4. Student 根据密集的 Token 级奖励继续更新。

核心奖励比较 Teacher 和 Student 对当前 Token 的概率。用更容易阅读的等价写法表示：

```text
教师优势 = log P_teacher(yₜ | 任务域, effort, 上下文)
         − log P_student(yₜ | effort, 上下文)

rₜᴼᴾᴰ = clip(stop_gradient(教师优势), −Rmax, Rmax)
```

因为 rollout 来自 Student 当前分布，它比纯离线蒸馏更不容易遇到分布偏移。最终得到的统一 K3 既保留领域能力，也支持 effort 控制。

### Partial Rollout：别让所有 GPU 等最慢的 Agent

Agent 任务的完成时间差异很大。同步训练如果必须等齐整个 batch，就会被最长尾拖住。

K3 的 Partial Rollout 允许完成 `λNK` 条轨迹后就触发优化。没完成的任务不会被扔掉，也不会被截断后硬算奖励，而是把模型状态和沙箱状态保存下来，下一轮继续跑。

收益是吞吐提高；代价是系统必须管理持久化状态，以及 stale / off-policy 风险。

系统还会约束成本：

- 当预算超过阈值时，奖励直接变为 `−1`；
- 预算课程从 Max 逐步收紧到 High 和 Low。

这说明 K3 优化的不是“只要做对就行”，而是“在给定成本内做对”。

### Agentic GRM：不能硬验时，也别只凭感觉打分

有些工作产品很难用单一测试脚本验证，例如研究报告、复杂方案或综合分析。K3 的做法是：

```text
读取任务和产物
  → 生成任务自适应 Rubric
  → Scorepad 逐项记录证据
  → Group Tournament 做二元比较
  → 聚合奖励
```

Rubric 可以覆盖事实准确性、证据质量、完整性、专业性、风险披露和表达效率。

但软评审依然有风险：Rubric 可能漏约束，Judge 可能看不见隐藏环境错误，多个评审也可能共享同一种偏差。

所以 K3 的奖励哲学可以概括成一句话：

> 能硬验证就硬验证；实在不能硬验证，再把软评审过程结构化。

---

## 六、训练和服务不是两个世界

很多模型是先按高精度训练，最后部署时再想办法量化。K3 把这件事前移到了 SFT：

```text
Pretraining：高精度训练
  → SFT：开启 QAT
  → RL / MOPD：在量化形态下继续学习
  → Serving：MXFP4 W / MXFP8 A
```

这样做有几个直接收益：

- 后训练更早接触部署时的数值误差；
- 超大 MoE 权重占用显著下降；
- 减少 post-hoc quantization 的能力回退；
- rollout 策略更接近线上实际策略。

当然，难点也不少：路由和 latent 激活对量化误差敏感，RL reward 对小变化敏感，不同层型的数值范围又不一致。

训练栈则围绕两个主要瓶颈搭建：

- **FlashKDA**：融合 KDA 递推与矩阵计算，并支持 context parallelism；
- **MoonEP**：为超大 MoE 做专家并行、动态冗余、zero-copy 和自适应调度；
- **Pipeline / ZeRO**：处理参数分片、流水线气泡和通信重叠。

服务端再加三件套：

1. **EAGLE-3**：`unroll = 7`，一次 Target forward 尽量接受更多草稿 Token；
2. **Unified Prefix Cache**：统一索引 KDA recurrent state 和 MLA KV prefix；
3. **State-aware Reuse**：复用缓存时检查混合层状态，并用实例亲和保持长会话局部性。

所以 1M Agent 服务的成本，不只取决于每 Token FLOPs，更取决于状态能不能复用、请求怎么调度，以及长会话能否留在合适的实例上。

> **系统判断**：K3 从 SFT 起就让模型适应部署数值环境，再用缓存和调度承接长会话。这比“训练完再想怎么压缩和上线”更接近端到端共设计。

### 从华为昇腾架构看 K3 的量化设计

从数值计算视角看，K3 的低精度设计与昇腾计算架构之间存在多处技术呼应。

#### MXFP4 / MXFP8 与昇腾低比特计算

K3 采用 **MXFP4 权重 + MXFP8 激活**。在昇腾产品体系中，Ascend 950PR 对应的 Atlas 350 已通过 [CANN 9.0](https://www.hiascend.com/document/detail/zh/CANNCommunityEdition/900/releasenote/release-notes.md) 公开支持 MXFP4、MXFP8 和 FP8，并提供低比特矩阵乘、`RmsNormQuant` 等相关算子能力。

MX 格式的核心不是单纯把浮点数截短，而是让一组元素共享 Scale 或指数。以 CANN 的 MXFP4 定义为例，线性层权重沿最后一维每 32 个元素共享一个指数。相比整个 Tensor 共用一个 Scale，microscaling 能更细地跟踪局部数值范围，尤其适合大模型权重中不同通道、不同专家差异很大的场景。

从硬件执行角度看，数据类型名称只是第一层。MXFP4 是否真正产生性能收益，还与 Scale 粒度、权重布局、矩阵形状，以及量化和反量化能否与矩阵乘融合有关。如果低比特计算前后频繁发生格式转换，理论上的带宽和算力收益会被额外开销抵消。

#### 量化收益主要来自 Cube，数值风险集中在 Vector 路径

昇腾 AI Core 同时包含面向矩阵计算的 Cube 单元和面向归一化、门控、逐元素运算的 Vector 单元。K3 的大规模线性层、专家 FFN 和投影矩阵适合利用低比特 Cube 计算；Router、RMSNorm、Softmax、门控和递推状态更新则更依赖 Vector 或混合流水。

因此，K3 的“MXFP4 W / MXFP8 A”不能理解为所有中间张量都以 4 bit 或 8 bit 执行。更准确的理解是：让计算量最大、访存量最高的矩阵乘进入低精度通路，同时让决定数值稳定性和离散路由结果的控制路径保留更高精度。这种异构精度设计与昇腾 Cube + Vector 的硬件结构是匹配的。

#### KDA：投影可以低精度，递推状态更看重长期稳定性

KDA 中的 Q/K/V 投影和输出投影仍是规则矩阵乘，能够直接受益于低比特权重与激活。真正敏感的是固定状态 `S_t`：

- `S_t` 会跨 Token 持续递推，误差不是一次性的，而是可能被后续状态反复读取；
- `alpha_t` 控制遗忘速度，微小误差会改变信息保留的时间尺度；
- `beta_t` 与误差外积共同决定本次写入强度，精度扰动可能逐步改变状态方向；
- `RMSNorm` 和输出门负责约束递推读出的幅值，属于稳定状态分布的重要环节。

所以 KDA 的量化重点不是单次矩阵乘的误差，而是长序列递推后的状态漂移。对于 1M 上下文，短序列上的输出余弦相似度正常，并不能充分说明 `S_t` 在长期演化中仍然稳定。

从昇腾执行特征看，KDA 也体现出明显的 Cube / Vector 混合负载：投影计算偏 Cube，通道衰减、门控、状态修正和归一化偏 Vector。低比特只加速前者时，整层性能仍会受到后者占比和片外访存的影响。

#### MLA：权重量化与 latent cache 量化是两件事

Gated MLA 先把每个 Token 的 K/V 压缩到 512 维 latent，再由 96 个头恢复各自的 key 和 value。这里存在两类不同的低精度对象：

- `W_c`、各头 K/V 恢复矩阵和输出投影属于权重矩阵，可使用低比特矩阵乘；
- `c_t_KV` 是随序列增长的运行时缓存，是否压缩取决于激活分布和 per-token Scale 的稳定性。

权重量化主要减少模型存储和矩阵乘带宽；latent cache 量化主要减少长上下文的缓存容量与访存。二者的精度风险也不同：前者影响每次投影，后者会影响所有后续 Token 对历史位置的读取。

注意力分数和 Softmax 通常仍需要较高精度。1M 上下文下，Softmax 要在大量历史位置之间区分接近的分数；若 score 的动态范围被过度压缩，问题不只是输出产生小误差，而是模型可能选择了错误的历史证据。

#### MoE：4 bit 最有价值的区域，也是 Scale 最复杂的区域

K3 拥有 896 个 routed experts，每个 Token 只激活 16 个。专家 FFN 占据绝大多数参数，因此 4 bit 权重对模型容量和权重带宽的影响最大。MindIE 在 [DeepSeek W4A8 混合量化](https://www.hiascend.com/document/detail/zh/mindie/22RC1/mindiellm/llmdev/mindie_llm0516.html) 中采用 routed experts W4A8、MLA 与 shared experts W8A8，并组合 per-channel 与 per-group Scale，也体现了同样的数值规律：路由专家适合更激进地压缩，公共路径则更强调稳定性。

不过，896 个专家不应共享一套粗粒度量化参数。不同专家处理的 Token 类型不同，权重和激活分布也会分化。per-expert、per-channel、per-group 和 per-token 等粒度，本质上是在量化元数据开销与专家数值保真之间取平衡。

Router logits、Top-K 排序和 Quantile Balancing 统计量则属于高敏感控制路径。K3 是从 896 个专家中选择 16 个，候选专家之间的分数差可能很小；量化误差一旦改变 Top-K 集合，就会同时改变模型计算内容和专家负载。这里的风险不能仅用某个张量的平均量化误差衡量。

最后还要看到，专家权重变成 4 bit，主要降低的是权重存储和专家计算的访存压力，并不会自动减少 Token dispatch / combine 的通信量。对于超大 MoE，低比特计算与专家并行通信仍然是两条相对独立的性能边界。

> **昇腾技术判断**：K3 的量化不是全模型统一降精度，而是“矩阵乘低比特、递推状态高稳定、路由控制高精度、缓存单独评估”的异构数值系统。它与昇腾 Cube + Vector 的计算结构具有较强呼应；最终效果取决于低比特矩阵乘占比，以及 Scale、归一化、门控和状态更新能否共同保持数值稳定。

---

## 七、放到同行里看：K3 已进第一梯队，但不是全胜

### 国内模型的架构与规格

| 维度 | Kimi K3 | DeepSeek-V4 Pro | GLM-5.2 | Qwen3.6 |
|---|---|---|---|---|
| 参数 | 2.8T / 104.2B | 1.6T / 49B | 744B / 40B | 35B-A3B / 27B |
| 注意力 | KDA + Gated MLA | CSA + HCA | Sparse attention / IndexShare | Gated Delta Network |
| MoE | 896，激活 16+2 | 384，激活 6+1 | MoE，A40B | 35B-A3B 版本 |
| 上下文 | 1M | 1M | 1M | 262K |
| 开放性 | 开放权重 | 开放权重 | 开放权重 | Apache 2.0 |

### 国内模型的训练披露

| 维度 | Kimi K3 | DeepSeek-V4 | GLM-5 / 5.2 | Qwen3.6 |
|---|---|---|---|---|
| 预训练规模 | 未披露；文本+视觉早融合 | Pro：33T+ | 28.5T；长上下文 mid-train | 数万亿多模态 Token；精确值未披露 |
| SFT / RL | SFT → 3×3 RL → MOPD | 领域专家 + GRPO → 多教师 OPD | SFT + GRPO / IcePop + 异步 Agent RL | Progressive async RL |
| Agent 轨迹与环境 | 白盒 harness、KG 合成、AET、微虚机、1M RL | DSec sandbox、Token WAL、百万上下文 RL / OPD | 超过 10K SWE、终端、2M 页面图、TITO 对齐 | Million-agent environments，配方粒度较低 |

按公开技术细节的完整程度来看：

> K3 ≈ DeepSeek ≈ GLM ＞ Qwen

注意，这只是披露程度，不等于真实投入或能力排名。

### 国外模型：产品规格公开，内部配方仍是黑箱

| 维度 | Gemini 3.1 Pro | GPT-5.6 Sol | Claude Fable 5 | Claude Opus 4.8 |
|---|---|---|---|---|
| 架构 / 参数 | 未披露 | 未披露 | 未披露 | 未披露 |
| 输入模态 | 文本 / 图像 / 视频 / 音频 / PDF | 文本 / 图像 | 文本 / 图像 | 文本 / 图像 |
| 上下文 | 1M | 1M | 1M | 1M |
| 输出上限 | 65,536 | 128K | 128K | 官方文档依版本 |
| 推理控制 | Thinking / tools | none → max；ultra 为多 Agent 编排 | 自适应 / 长时 Agent | Adaptive thinking |
| 开放性 | 闭源 API | 闭源 API | 闭源 API | 闭源 API |

### 代表性能力基准

| 基准 | K3 | Fable 5 | GPT-5.6 | Opus 4.8 | GLM-5.2 |
|---|---:|---:|---:|---:|---:|
| GPQA | 93.5 | 92.6 | **94.1** | 91.0 | 91.2 |
| HLE + tools | 56.0 | **63.0** | 58.0 | 57.9 | — |
| Terminal-Bench 2.1 | 88.3 | 88.0 | **88.8** | 84.6 | 82.7 |
| FrontierSWE | 81.2 | **86.6** | 71.3 | 66.7 | 67.3 |
| MMMU-Pro + Python | 83.4 | **86.5** | 84.6 | 82.7 | — |
| OmniDocBench | **91.1** | 89.8 | 85.8 | 87.9 | — |

#### 这些评测数据集分别在测什么

- **GPQA**：由生物、物理和化学领域专家编写的研究生级多选题。原始数据集包含 448 题，并刻意设计成非专业人员即使可以上网也很难快速解答，用于测试高难专业知识和推理。K3 的公开成绩仅标注 GPQA，没有说明具体子集和运行协议。[GPQA 论文](https://arxiv.org/abs/2311.12022)
- **HLE + tools**：Humanity's Last Exam 覆盖大量学科的专家级、封闭式、可验证问题，并包含多模态题目；`+ tools` 表示评测时允许模型使用工具，更强调检索与推理协作。K3 没有展开具体工具和 harness 配置。[HLE 论文](https://arxiv.org/abs/2501.14249)
- **Terminal-Bench 2.1**：让 Agent 在真实命令行环境中完成可验证任务，考察终端操作、依赖处理、调试和长程执行。2.1 是对 2.0 的修订版，修复了 89 个任务中的 28 个，并增加持续验证机制。[Terminal-Bench 2.1 官方说明](https://www.tbench.ai/news/terminal-bench-2-1)
- **FrontierSWE**：面向人类能力边界附近的开放式、超长程软件工程任务，覆盖复杂实现、性能优化和研究型工程问题。它比短代码题更依赖完整工程环境、长时间执行和隐藏验证。[FrontierSWE 官网](https://www.frontierswe.com/)
- **MMMU-Pro + Python**：MMMU-Pro 从大学层级多学科视觉题出发，过滤掉可由纯文本模型猜对的题，增加候选项，并引入把题目文字也放进图像的 vision-only 设置。`+ Python` 表示评测允许使用 Python 工具，但具体调用协议没有公开。[MMMU-Pro 论文](https://arxiv.org/abs/2409.02813)
- **OmniDocBench**：面向真实 PDF 文档解析，覆盖论文、财报、报纸、教材和手写笔记等多种文档类型，考察版面、表格、公式、OCR 与阅读顺序。版本变化会影响数据规模和分数，比较时应确认具体版本。[OmniDocBench 官方仓库](https://github.com/opendatalab/OmniDocBench)

这张表透露出一个比较清晰的能力轮廓：

- K3 在终端、文档视觉和部分 SWE 任务上很强；
- 在 HLE 和部分高难知识、推理任务上，并不是最领先的；
- 它已经进入第一梯队，但不是“每一项都赢”。

### Agent 基准

| 基准 | K3 | Fable 5 | GPT-5.6 | Opus 4.8 | GPT-5.5 |
|---|---:|---:|---:|---:|---:|
| BrowseComp | **91.2** | 88.0 | 90.4 | 84.3 | 84.4 |
| MCPMark Verified | **94.5** | 87.4 | 92.9 | 76.4 | 92.9 |
| AutomationBench | **30.8** | 29.1 | 29.7 | 27.2 | 22.7 |
| OfficeQA | 63.3 | **69.9** | 63.2 | 63.9 | 60.9 |
| OSWorld Verified | 84.8 | **85.0** | 83.0 | 83.4 | 79.0 |
| Agents Last Exam | 28.3 | 25.7 | **29.6** | 27.0 | 26.6 |

#### 这些 Agent 数据集分别在测什么

- **BrowseComp**：包含 1,266 个需要持续浏览的难题，答案通常隐藏在多个网页和相互纠缠的信息中。它考察的不是普通搜索，而是能否跨站点定位罕见事实、整合证据并坚持搜索。[OpenAI BrowseComp 介绍](https://openai.com/index/browsecomp/)
- **MCPMark Verified**：MCPMark 使用带初始状态的真实数字环境和程序化验证脚本，任务涉及创建、读取、更新和删除等多轮工具操作；Verified 是其中经过稳定化和版本锁定的子集，目的是提高可复现性。[MCPMark 官网](https://mcpmark.ai/)
- **AutomationBench**：面向销售、营销、运营、客服、财务和人力资源等真实业务流程。Agent 需要跨 CRM、邮箱、日历等模拟 SaaS 工具协作，最后根据系统终态是否正确进行程序化评分。[AutomationBench 官方仓库](https://github.com/zapier/AutomationBench)
- **OfficeQA**：考察 Agent 能否在大量真实办公文档中完成检索、解析和数值推理。OfficeQA Pro 使用近百年的美国财政部公报，包含密集表格、图表和正文；K3 的公开成绩只标注 OfficeQA，具体版本仍需结合评测配置理解。[OfficeQA 官方仓库](https://github.com/databricks/officeqa)
- **OSWorld Verified**：在真实操作系统和应用程序中评估多模态电脑操作 Agent，任务通过环境设置和执行结果评分。Verified 版本修复了社区报告的问题，提高了任务与评估脚本的一致性。[OSWorld 官方网站](https://osworld-v1.xlang.ai/)
- **Agents' Last Exam**：面向长程、经济价值较高且结果可验证的真实职业任务，覆盖 13 个行业集群、55 个子领域和 1,000 多项任务，用来观察 Agent 是否真的能完成专业工作，而不仅是回答知识题。[Agents' Last Exam 论文](https://arxiv.org/abs/2606.05405)

K3 的优势明显集中在 Browse、MCP、自动化和操作系统等“真的要动手”的任务上。这与它强调白盒环境、可执行轨迹和真实终态验证是吻合的。

不过还是要提醒：Agent 成绩与工具配置、上下文管理、推理预算和 scaffold 高度相关，不应直接理解为裸模型排名。

---

## 八、别只看优点：K3 还有五个现实问题

### 1. 固定状态记忆一定会有取舍

KDA 的优势是缓存不随序列增长，但固定容量也意味着某些精确历史可能丢失。Gated MLA 是补偿机制，不代表取舍消失了。

### 2. 2.8T 权重依然很难运维

激活参数少，不等于总权重不存在。存储、加载、通信、部署、更新和版本管理都仍然很重。

### 3. 原生视觉不等于所有多模态任务都领先

K3 的视觉—Agent 闭环很有价值，但在部分高难基准上仍落后最强闭源模型。闭源产品和生态的迭代速度也不能忽略。

### 4. Verifier 会把自己的盲区带进训练

任务合成和 RL 越依赖验证器，验证器质量就越像天花板。Rubric 漏项、隐藏测试不完整、奖励可被投机，都会把偏差规模化。

### 5. 训练规模仍不透明

没有总 Token、FLOPs 和集群规模，就很难独立判断所谓 2.5× scaling efficiency 到底来自哪里，也难估算复刻成本。

---

## 九、如果团队想学 K3，先别急着堆参数

如果团队希望落地类似路线，可以沿着这条能力链推进：

```text
任务资产
  → 统一 Harness
  → Verifier
  → Sandbox
  → 异步 RL
  → 多教师合并
```

这条顺序值得重视，因为前面的每一项都是后面的地基。

### 前 90 天：先做出能重放、能验收的任务

- 选 3 类高价值业务流程；
- 建立 100–500 个可重放任务；
- 为每个任务明确初始状态、目标、预算和允许工具；
- 建立隐藏验证器，避免模型只会迎合公开规则。

这阶段不要追求“任务数量看起来很大”。100 个能稳定复现和验证的任务，通常比 10 万条无法重放的聊天记录更有价值。

### 到 180 天：统一环境和轨迹资产

- 统一工具 schema；
- 统一状态快照和轨迹版本；
- 上线异步回放；
- 建立失败分类；
- 记录环境、镜像、工具和 verifier 版本；
- 验证同一任务能否跨不同 harness 配置运行。

这一阶段的目标，是让轨迹从一次性样本变成可以持续调试、回放和重新评分的工程资产。

### 到 360 天：训练专家，再考虑统一

- 按领域和预算训练 specialist；
- 使用 on-policy distillation 合并；
- 建立成本—质量前沿；
- 从答案任务逐步扩展到多阶段可执行工作流。

如果前两阶段没做扎实，直接进入大规模 RL，往往只是更快地放大奖励漏洞和环境噪声。

---

## 最后：K3 的护城河可能不是模型，而是闭环

回头看，K3 的完整逻辑其实很连贯：

- KDA、MLA 和 AttnRes 让模型能处理更长、更深的信息流；
- 超大稀疏 MoE 提供容量，同时控制每 Token 的激活成本；
- MoonViT-V2 让截图、页面、视频和渲染结果进入统一上下文；
- XTML 把思考、工具调用和环境反馈统一为可学习序列；
- 白盒环境、任务合成、AET、异步 RL 和 GRM 负责生产可执行、可验证的轨迹；
- MOPD 把多个领域和预算专家重新合并成一个可部署模型；
- QAT、EAGLE-3、统一缓存和状态复用把训练结果送到真实服务系统。

所以，如果一定要用一句话概括：

> K3 的关键不是“模型有多大”，而是它能不能持续生产真实任务、真实动作、真实反馈和真实奖励。

对想复刻这条路线的团队，优先级也应该是：

> **Verifier 质量 > 环境可复现 > 任务多样性 > 轨迹数量 > 参数规模**

参数规模最显眼，却可能是整条链里最晚才该扩大的那一项。
